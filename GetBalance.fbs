include "./common.fbs";

namespace WebView.EthersCommand;

table GetBalanceInput {
  id:int16;
  provider_key:string;
  address:string;
}

table GetBalanceOutput {
  id:int16;
  provider_key:string;
  balance:BigInt;
}